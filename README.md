# pinched-hysteresis

PINCHED HYSTERESIS — the memristor's fingerprint: an i-v loop that pinches at the origin (HP drift model). A runnable 2D-canvas companion to the memristor device sphere. HEUREMA.

Self-contained interactive 2D-canvas page (no external dependencies). ROOT0, with AVAN. CC-BY-ND-4.0.
